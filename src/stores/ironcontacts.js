import { ref, computed} from 'vue';
import { defineStore } from 'pinia';

export const useIronContactsStore = defineStore('ironcontacts', () => {
    //State
    const noSe = ref(0);

    //computed
    const noSeTpoco = computed (() => );

    //Actions 
    function noSeAhora() {
        hacerAlgo
    }

    return algo
})